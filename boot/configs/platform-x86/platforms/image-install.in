## SECTION=image2

config IMAGE_INSTALL
	bool
	select IMAGE_KERNEL
	select HOST_GENIMAGE
	select HOST_DOSFSTOOLS
	select HOST_MTOOLS
	prompt "Generate images/install.img"
	help
	  Generate install image
