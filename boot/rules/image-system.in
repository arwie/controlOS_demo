## SECTION=image2

config IMAGE_SYSTEM
	bool
	select IMAGE_ROOT_TGZ
	select IMAGE_KERNEL
	select HOST_GENIMAGE
	select HOST_DOSFSTOOLS
	select HOST_MTOOLS
	prompt "Generate images/system.img.xz"
	help
	  Generate system image
