## SECTION=image2

config IMAGE_INSTALL
	bool
	select IMAGE_SYSTEM
	select HOST_GENIMAGE
	select HOST_DOSFSTOOLS
	select HOST_MTOOLS
	prompt "Generate images/install.img.xz"
	help
	  Generate install image
